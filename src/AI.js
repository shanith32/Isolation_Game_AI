// Get the children of a state
function getChildren(state) {
  // Get a list of all children for the given state
}

// Static Evaluation Function
function SEF(state) {
  // Difference between the available move for p1 and p2
}

// Minimax search algorithm
function minimax(currentState, depth, maximizingPlayer) {}

export default minimax;
